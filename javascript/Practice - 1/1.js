console.log("Array Practice!");

list = [100, 200, 500];
console.log(list);
// list.pop --> Removes the last element from the array. 
// list.shift(); --> Removes the first element from the array.
// list.unshift(100); --> Add a new element to the first position of the array.
// list.push(100); --> Add a new element to the last position of the array.

console.log(list);
